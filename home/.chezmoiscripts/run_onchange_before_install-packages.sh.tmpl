#!/usr/bin/env bash

set -eufo pipefail

{{ $brews := list
    "dockutil" -}}

{{ $casks := list
    "1password-cli" -}}

brew bundle --file=/dev/stdin <<EOF
{{ range ($brews | sortAlpha | uniq) -}}
brew "{{ . }}"
{{ end -}}
{{ range ($casks | sortAlpha | uniq) -}}
cask "{{ . }}"
{{ end -}}
EOF
